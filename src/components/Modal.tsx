import {useEffect,useRef,type ReactNode} from 'react';import {X} from 'lucide-react';
export default function Modal({title,onClose,children}:{title:string;onClose:()=>void;children:ReactNode}){const ref=useRef<HTMLDialogElement>(null);useEffect(()=>{const prev=document.activeElement as HTMLElement;ref.current?.showModal();return()=>prev?.focus()},[]);return <dialog ref={ref} className="modal" onCancel={onClose} onClick={e=>{if(e.target===ref.current)onClose()}} aria-label={title}><div className="modal-inner"><button className="circle close" aria-label="Close dialog" onClick={onClose}><X/></button>{children}</div></dialog>}
